% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Run_CONQUER.R
\name{SuSiEConquer}
\alias{SuSiEConquer}
\title{SuSiE from conquer smoothed quantile IRLS sufficient statistics}
\usage{
SuSiEConquer(
  X,
  Y,
  Z = NULL,
  tau = 0.5,
  kernel = c("parabolic", "Gaussian", "logistic", "uniform", "triangular"),
  h = 0,
  checkSing = FALSE,
  tol = 1e-04,
  iteMax = 5000,
  stepBounded = TRUE,
  stepMax = 100,
  L = 10,
  max.iter = 5,
  min.iter = 2,
  max.eps = 1e-04,
  susie.iter = 100,
  verbose = TRUE,
  n_threads = 1,
  coverage = 0.9,
  estimate_residual_variance = TRUE,
  residual_variance = NULL,
  residual_variance_init_scale = 0.5,
  residual_variance_bounds = c(0.5, 2),
  fi_floor_quantile = 0,
  fi_floor_min = 0,
  fi_normalize = TRUE,
  fi_normalize_method = c("mean", "median"),
  keep_zero_weight = TRUE,
  projection_solver = c("ginv", "ridge"),
  susie_n = c("n_eff", "n", "n_active"),
  scaled_prior_variance = 1,
  L.init = 1,
  refit_noncs = TRUE,
  noncs_var = 0.2,
  suff_block_size = 10000L,
  ...
)
}
\arguments{
\item{X}{Numeric n by p matrix of main variables.}

\item{Y}{Numeric response vector.}

\item{Z}{Optional nuisance covariate matrix.}

\item{tau}{Quantile level passed to \code{conquer::conquer()}.}

\item{kernel}{Kernel passed to \code{conquer::conquer()}.}

\item{h}{Bandwidth passed to \code{conquer::conquer()}; non-positive values
use conquer's default rule.}

\item{checkSing, tol, iteMax, stepBounded, stepMax}{Main fitting arguments
passed to \code{conquer::conquer()}.}

\item{L, max.iter, min.iter, max.eps, susie.iter}{Main SuSiE iteration controls.}

\item{residual_variance}{Initial SuSiE residual variance. If \code{NULL},
uses \code{residual_variance_init_scale * v0}.}

\item{residual_variance_init_scale}{Multiplier for the initial residual
variance when \code{residual_variance} is \code{NULL}.}

\item{residual_variance_bounds}{Multipliers for lower and upper SuSiE
residual variance bounds relative to \code{v0}.}

\item{fi_floor_quantile}{Optional lower quantile used to floor positive
kernel densities after optional normalization. The default \code{0} does
not clip.}

\item{fi_floor_min}{Optional absolute lower bound for positive kernel
densities after optional normalization. The default \code{0} does not clip.}

\item{fi_normalize}{If \code{TRUE}, rescale positive kernel densities.}

\item{fi_normalize_method}{Standardize positive kernel densities by their
mean or median.}

\item{keep_zero_weight}{For compact kernels, keep exact zero densities as
zero working weights.}

\item{projection_solver}{Solver for the weighted Schur projection.}

\item{susie_n}{Sample size passed to \code{susieR::susie_ss()}. Use
\code{"n_eff"} for the weighted effective local size, \code{"n"} for the
original sample size, or \code{"n_active"} for the number of nonzero-weight
observations.}

\item{...}{Additional arguments passed to \code{susieR::susie_ss()}.}
}
\description{
SuSiE from conquer smoothed quantile IRLS sufficient statistics
}
